/** Qual programa esta build do app atende. Um código, várias builds — pra
 * gerar a build do Aventureiros+ no futuro, troca esses dois valores. */
export const PROGRAMA_APP_ID = 1;
export const PROGRAMA_APP_CODIGO = 'desbravadores' as const;
